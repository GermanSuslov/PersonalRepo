package org.ExchangeTest;

public class ExchangeRatesTest {/*

    public void test891() {
        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("dd.MM.yyyy");
        LocalDate date = LocalDate.now();
        date = date.plusDays(1);
        String dayOfWeek = date
                .getDayOfWeek()
                .getDisplayName(TextStyle.SHORT, new Locale("ru"));
        dayOfWeek = dayOfWeek.substring(0, 1).toUpperCase() + dayOfWeek.substring(1);
        dayOfWeek += " " + date.format(formatter);
        String correct= dayOfWeek + " - " + 3.21;
        Prediction predictionTest = new Prediction("TRY", "tomorrow");
        ArrayList<String> testedList = predictionTest.getPrediction();
        assertEquals(correct, testedList.get(0));
    }*/
}
